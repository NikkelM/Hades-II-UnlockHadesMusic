local config = {
  version = 0;
  enabled = true;
  unlockEverything = false;
}

local configDesc = {
  enabled = "Whether the mod is enabled or not.";
  unlockEverything = "NOT REVERSIBLE! When set to true, will immediately unlock all songs and skip meta progression built into the mod."
}

return config, configDesc